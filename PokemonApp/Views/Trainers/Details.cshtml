@model PokemonApp.Models.Trainer

@{
    ViewData["Title"] = "Details";
}

<div>
    <h4>Trainer</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dd class = "col-sm-10">
            
        </dd>@foreach (var c in Model.Catches)
        {
            <dt class="col-sm-2"></dt>
            <dd class = "col-sm-10">@Html.DisplayFor(modelItem => c.Name)</dd>
        }
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>

    <a asp-controller="Catch" asp-action="Create" asp-route-id="@Model.Id">Catch a New Pokemon</a> 
</div>
